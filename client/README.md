# Client (React + Vite)

Run the client:

```powershell
cd client
npm install
npm run dev
```

The dev server runs on `http://localhost:5173` and proxies API calls to `/api/v1` (assumes backend is running on same host).
